spring:
  application:
    name: configserver

  profiles:
    active: git

  cloud:
    config:
      server:
        git:
          uri: https://github.com/sohan91/MicroServices-Yaml-Config-Files
          default-label: master
          timeout: 5
          clone-on-start: true
          force-pull: true

  rabbitmq:
    host: "localhost"
    port: 5672
    username: "guest"
    password: "guest"

server:
  port: 8071

encrypt:
  key: "3wue27e37iriqubd732@040923#"
management:
  endpoints:
    web:
      exposure:
        include: "*"
